# InflationCompair
A chrome extension that compaired ETFs that say that they are secured against inflation. Required disabling same-origin access policy

Uses yahoo fiance data and Nasdaq data link
(Broken Yahoo no longer allows users to query their API for CSV files)

